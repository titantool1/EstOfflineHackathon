# 새 원본 063828f 프론트 통합

원본 저장소 `codex/mission-chatbot-integration`의 `063828f1d49de48969f23456632ae260d4403839`를 화면 기준으로 반영했다. 로그인 좌우 구성, 관심사 선택/수정, 프로필 카드, 홈 미션 미리보기, 미션 시작→자기 확인→완료를 현재 회원·미션 API에 연결했다. 일별 샘플 통계는 실제 누적 고유 미션 수와 레벨로 표시한다. 서버 저장 성공 후에만 완료를 표시하며 공식 포인트 지급을 대신 판정하지 않는다.

## 배포와 검사

- 이 작업 배포: `eco-jupjup-team-frontend:qa-front-063828f-20260918`. 다른 네 서비스는 유지했다.
- 이후 동시 지도 작업이 새 UI를 보존한 `qa-map-district-20260918` 웹/백엔드를 배포했다. 현재 재기동은 최신 지도 배포 기록과 실행 컨테이너의 Compose 구성을 따른다. 이 작업의 이전 웹만 다시 올리면 후속 지도 수정이 빠진다.
- 배포본 전체 테스트197, 린트·타입·빌드, API 대역 브라우저36흐름, 320/390/768/1440 너비 검사 통과.
- 실제 보호 QA: 가입·자동 로그인, 저장 동네 표시, 관심사 수정/프로필 복귀, 미션 시작/완료 후 누적1·Lv.2, 홈 미리보기, 미션→챗 질문/복귀 확인. 정책20개 카드/상세와 원문 혜택 보존, 지도40/성동구 개인컵13/빈검색 확인. 합성 회원 잔여0.
- 후속 지도 배포 이후 공개 공유 주소의 홈·로그인·미션에서 새 화면이 유지되는 것도 확인했다. 배포 중 일시502였던 첫 시도와 구분한다.

## 병합 소스와 후속

제품 소스에는 동시 작업의 완료상태 복원, 동네 인증 만료 복귀, 구 검색/전체 초기화도 보존해 새 UI와 합쳤다. 이 작업이 처음 배포한 이미지에는 이 후발 변경이 없다. 완료상태 복원은 대응 백엔드와 함께 후속 배포해야 한다. 후속 작업은 새063828f UI가 반영된 최신 제품 소스를 기준으로 진행하고 별도 작업본의 옛 화면을 덮어쓰지 않는다. 전체 main 변경은 미커밋이며 이번 push는 없다.

검사한 릴리스·원본 화면 비교·해시·배포/원복 명령·실제 QA 결과는 작업대의 `evidence/eco-frontend-063828f-2026-09-18/`에 보존했다. 초기 웹의 원복 명령은 `rollback-command.json`, 해당 배포는 `deploy-result.json`, 공유 주소 확인은 `public-result.json`이다. 후속 배포 이후에는 그 배포의 원복 기준을 사용한다.
