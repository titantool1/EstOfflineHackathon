# 최종 QA 후속 통합·원격 공유

2026-09-18 사용자 승인으로 `main`의 `5204568` 이후 완료된 수정들을 `work/qa-final-push-20260918`에서 고정하고 main에 통합·일반 push하는 범위다. 이전 `9ece89c`·`5204568`의 기능도 함께 공유한다. 강제 push·새 DB 이관·재배포는 수행하지 않는다.

추가 변경은 메뉴 순서(홈→챗봇→미션→지도), 로그아웃 성공 시 로그인/가입 진입, 지도에서 현재 회원의 저장 관심동네 시·군·구 기본 적용, 동네 저장 후 관심사 유도 블록 제거다. 명시 지역검색·전체초기화·재진입·비회원/조회실패 기본값·늦은 응답 취소와 기존 로그인 복귀를 유지한다. 지도 조회는 현재 요청의 회원 쿠키로만 동네를 읽고 공용 브라우저 저장소나 공개 장소 요청으로 전달하지 않는다.

최종 고정 프론트216파일은 실행 중 `qa-neighborhood-cta-remove-20260918`의 소스 manifest와 일치한다. 동일 소스로 전체254검사·전체 src/tests 린트·타입을 확인하고 프로덕션 빌드 근거를 연결했다. 백엔드 변경은 없으므로 `qa-condition-memory-20260918`의 실제PG112/skip0와 조건 메모리 실제8턴/20항목 검사를 재사용한다. 검사 수는 판본별로 읽으며 합산하지 않는다.

실제 QA 근거: [메뉴 PC/모바일](../../codex-harness-lab/evidence/eco-nav-chat-order-2026-09-18/README.md), [로그아웃 실제4/실패대역1](../../codex-harness-lab/evidence/eco-logout-entry-2026-09-18/README.md), [지도 실제 저장/목록7항목·제어UI](qa-map-neighborhood-2026-09-18.md), [동네 버튼 제거 제어UI](qa-neighborhood-cta-remove-2026-09-18.md). 새 합성 회원·시연 계정·모델 호출은 추가하지 않았다.

지도 기본값 후속의 실제 SDK 타일은 로컬에서 미관측이며 외부 타일 검사를 통과했다고 주장하지 않는다. 이전 실제 SDK 검사는 당시 판본의 근거다. 이번 변경은 좌표/SDK 구현을 바꾸지 않았다. 닫힌 용기 판정 오류·나머지 정책 문구·음식용기 장소 데이터는 기존 후속 항목으로 남는다.

[실제 커밋·main 머지·push/원격 일치·임시 정리 결과](../../codex-harness-lab/evidence/eco-qa-final-push-2026-09-18/README.md). 개별 기능 문서의 미커밋/push없음은 당시 기록이며 현재 Git 상태는 이 결과를 따른다. QA 서비스와 병행 시연 계정 준비는 보존한다.
